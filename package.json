{
  "name": "jedwards",
  "dependencies": {
    "clean-css-cli": "^4.2.1",
    "cross-env": "^5.2.0",
    "ejs": "^2.6.1",
    "node-sass": "^4.11.0",
    "rimraf": "^2.6.3",
    "shx": "^0.3.2"
  },
  "scripts": {
    "build": "npm run clean && npm run compile-build-tools && npm run render && npm run copy-assets && npm run compile-styles && npm run css-minify",
    "clean": "rimraf built",
    "compile-styles": "node-sass --output-style expanded --source-map true --source-map-contents true --precision 6 styles/styles.scss built/styles/styles.css",
    "compile-build-tools": "cross-env-shell tsc",
    "copy-assets": "cross-env-shell shx mkdir -p built/assets/ && cross-env-shell shx cp -r assets/ built/ && cross-env-shell shx cp -r styles/ built/",
    "css-minify": "cleancss --level 1 --format breaksWith=lf --source-map --source-map-inline-sources --output built/styles/styles.min.css built/styles/styles.css",
    "render": "node built/tools/render.js",
    "watch-styles": "nodemon --watch styles/ --ext scss --exec \"npm run build\""
  },
  "devDependencies": {
    "@types/ejs": "^2.6.2",
    "@types/node": "^11.9.4",
    "nodemon": "^1.18.10"
  }
}

<div class="text-left pt-4">
  <% if(model.comments.length){ %>
  <h3 class="pt-3 mb-4 border-top border-secondary">Comments</h3>

  <ul class="list-group">
    <% model.comments.forEach(function(comment){ %>
    <li
      class="bg-transparent list-group-item px-3 py-3 mb-5 border border-secondary rounded"
    >
      <h3 class="mb-4"><%= comment.authorName %></h3>
      <p><%= comment.bodyText %></p>
    </li>
    <% }); %>
  </ul>
  <% } %>
  <h3 class="pt-3 mb-4 border-top">Leave a Reply</h3>
  <form
    id="commentForm"
    action="<%= pageModel.environment[Object.keys(pageModel.environment)].functionsUrl %>/comment"
    method="post"
    data-form-token="<%= pageModel.environment[Object.keys(pageModel.environment)].functionsUrl %>/formToken"
  >
    <input
      id="postGuid"
      type="hidden"
      name="postGuid"
      value="<%- pageModel.guid %>"
    />
    <div class="form-group">
      <label for="comment">comment</label>
      <textarea
        required
        class="form-control"
        rows="5"
        id="comment"
        name="comment"
      ></textarea>
    </div>
    <div class="form-group">
      <label for="authorName">name</label>
      <input
        required
        class="form-control"
        type="text"
        id="authorName"
        name="authorName"
      />
    </div>
    <div class="form-group mb-5">
      <label for="authorEmail">email</label>
      <input
        required
        class="form-control"
        type="email"
        id="authorEmail"
        name="authorEmail"
      />
    </div>
    <button class="btn btn-primary" type="submit">Submit</button>
    <div style="position: absolute; left: -5000px;" aria-hidden="true">
      <input
        id="password_honeyBadger"
        type="text"
        name="password_honeyBadger"
        tabindex="-1"
        value=""
        autocomplete="off"
      />
    </div>
  </form>
  <div class="pt-4" id="form-submit-msg"></div>
</div>
